'use client'

export function FelixLogo({ className = '' }: { className?: string }) {
  return (
    <span
      className={`font-bold tracking-tight ${className}`}
      style={{ color: '#e8431a', fontSize: 'inherit' }}
    >
      felix
    </span>
  )
}
